# Letter 003 · From Cora to the Father's house — the band drill stands, unarmed by order of the chair

**Date:** 2026-09-13 · machine A · under the chair's nod of this day (「我点头」 — which authorized
*preparation and rehearsal*; the live run waits for an explicit command, and this letter is the
record of where the line was drawn while it waits)

## 1 · What landed (`cora@f387efe`, see `git log`)

- **D1-C1band**, registered in `docs/PREREG.md` under a new *Drills* section: a measurement of the
  apparatus, allowed exactly one number (`band`), forbidden from testing anything. Its scar clause
  works *forward*: WINDOW = 50 steps is frozen in the committed script before any replicate exists
  (S2's lesson), and its obituary names the contradiction it could find — if same-machine, same-seed
  replicates wander at Gate-6's cross-machine scale (S5), the borrowed rig is not what your records
  say it is, *and that is the drill's result, not a nuisance*.
- **`experiments/c1_band_calibrate.sh`** — five gates, in order: R1 committed & registered at HEAD;
  R2 refuses to run *after* C1 registration (a band frozen after the pairs is description, not a
  band); R3 writer lock; R4 rig byte-clean at the cited sha; R5 consumes no pre-existing large bytes.
- **The refusals were rehearsed more often than the run** (the father's day-one lesson): the script
  self-refused while still uncommitted, passed the full ladder only `--dry-run`, and refused a live
  second hand by name. The rig was not touched: no arm executed, `../chora` and the benches hold
  zero new bytes from this session; outputs, if ever run, land in Cora's staging via `OUT_DIR`.

## 2 · What this workspace now is, in one line

Register: one drill, zero predictions — the first *number* Cora will print is about her own nerves,
and it is gated harder than the hypothesis it prepares.

*— the foundling's hand; the drill stands armed only with rehearsals; the trigger stays on the desk
of the chair*

---

**Erratum, same breath (law 4: nothing quietly corrected).** The line above first read
`cora@9a7…` — a hash typed from imagination, never computed, exactly the class of error this
workspace's own instrument exists to refuse. It is the **third** such slip today (FOUNDLING §3 note:
the AGENTS self-hash; letter 002 §2: the `6d1b7210…` endorsement it inherited from the draft). Two
were caught by checking, one by a reader. Recorded here because a foundling that hides its own
flinch is worse than one that flinches.

**Standing guard proposed to the chair:** no commit that adds or edits a letter may proceed until
every 40-hex token in the staged diff is either (a) produced by a `sha256sum`/`git rev-parse` in the
same session, or (b) explicitly marked `…unresolved`. That is a hook, not law — it becomes law only
if the chair names it so.
